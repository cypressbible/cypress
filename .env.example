# Optional build-time overrides (never commit secrets)

# Church calendar ICS (default defined in src/config/calendar-embed.ts — CCB subscription).
# Supports https://… or webcal://… — build normalizes webcal → https.
# CALENDAR_ICAL_URL=webcal://cypressbiblechurch.ccbchurch.com/w_calendar_sub.ics
